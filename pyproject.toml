[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "any-context"
version = "0.28.4"
description = "Transform any file, folder, website, or drive into a living, real-time AI context."
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Levi Silveira" }
]
dependencies = [
    "chromadb",
    "python-docx",
    "llama-index",
    "llama-index-vector-stores-chroma",
    "llama-index-readers-file",
    "llama-index-embeddings-openai",
    "docx2txt",
    "langchain",
    "langchain-openai",
    "langgraph-checkpoint-sqlite",
    "langgraph-cli",
    "pydantic",
    "questionary",
    "prompt-toolkit>=3.0.40",
    "duckduckgo-search>=7.0.0",
    "tqdm",
    "python-dotenv",
    "fastapi",
    "uvicorn",
    "langchain-anthropic>=1.5.6",
    "langchain-google-genai>=4.3.4",
    "langchain-groq>=1.1.3",
    "lancedb>=0.37.1",
    "pyarrow>=25.0.1",
]

[project.scripts]
actx = "any_context.cli.entrypoint:entrypoint"
anycontext = "any_context.cli.entrypoint:entrypoint"
any-context = "any_context.cli.entrypoint:entrypoint"
ac = "any_context.cli.entrypoint:entrypoint"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"any_context" = ["config/*.md"]
